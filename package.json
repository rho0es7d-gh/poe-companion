{
  "name": "poe-companion",
  "version": "1.0.0",
  "description": "Path of Exile companion app",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/index.html src/styles.css dist/",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "package": "npm run build && electron-builder"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "copyfiles": "^2.4.1",
    "electron": "^40.6.1",
    "electron-builder": "^26.8.1",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
